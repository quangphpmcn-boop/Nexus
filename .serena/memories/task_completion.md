# What to do when a task is completed

1. Run `/verify` workflow to check work
2. Run scope guard: `git diff` vs plan
3. Update `.nexus/state.md` with current position
4. Log usage to `.nexus/logs/`
5. Atomic commit with conventional format
6. Run `/guide` to show next steps
